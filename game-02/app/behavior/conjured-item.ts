import { ItemBehavior } from "./item-behavior";

export class ConjuredItem extends ItemBehavior {
    updateQualityBehavior(): void {
        super.updateQualityBehavior();
        super.updateQualityBehavior();
    }
}